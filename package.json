{
  "name": "kisko-bolun-up",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8888",
    "build": "node scripts/validate-data.mjs",
    "test": "node --test",
    "coverage": "c8 --include 'assets/resolve-core.js' --check-coverage --lines 97 --branches 97 --functions 97 --statements 97 node --test",
    "netlify:dev": "npx netlify dev",
    "deploy": "npx netlify deploy --prod"
  },
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
